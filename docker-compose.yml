services:
  mysql:
    image: mysql:8.0
    container_name: temcar-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: temcar
      MYSQL_USER: temcar
      MYSQL_PASSWORD: temcar
      MYSQL_ROOT_PASSWORD: root
      TZ: America/Sao_Paulo
    ports:
      - "3307:3306"
    volumes:
      - temcar_mysql_data:/var/lib/mysql
      - ./temcar.sql:/docker-entrypoint-initdb.d/01-temcar.sql:ro
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      # Sem isto o MySQL pode subir escutando apenas em IPv6 ([::]:3306); o
      # docker-proxy encaminha para o IPv4 do container e a conexao e recusada.
      - --bind-address=0.0.0.0

volumes:
  temcar_mysql_data:
